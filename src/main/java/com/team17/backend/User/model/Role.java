package com.team17.backend.User.model;

// Defines whether a user is a renter or an owner. Admin and accountant rights are separate as boolean fields in the User class.
public enum Role {
    RENTER,
    OWNER
}
